import React from "react";
import { metaObject } from "@/config/site.config";

export const metadata = {
  ...metaObject("Sign In"),
};

//add set new password here
const Signin = () => {
  return <></>;
};

export default Signin;
